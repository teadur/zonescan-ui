<%= form_with(model: scan, local: true) do |form| %>
  <% if scan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scan.errors.count, "error") %> prohibited this scan from being saved:</h2>

      <ul>
      <% scan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :scan_name %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :scan_time %>
  </div>

  <div class="field">
    <%= form.label :result %>
    <%= form.text_area :result, id: :scan_result %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
