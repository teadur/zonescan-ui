<h1>Admin interface</h1>
It's <%= Time.now %>

<p>We have <%= pluralize(@total_domains, "domain") %></p>
<p>We have <%= pluralize(@total_scans, "scan") %></p>
<p>We have <%= pluralize(@total_results, "result") %></p>
<p>Add new domain:<%= link_to 'New Domain', new_domain_path %></p>
<p>Scan all domains now: <%= link_to 'Scan all',  scan_all_path  %></p>
